<h2>You Are Currently Standing At...</h2>

<p>
  <%= @location.my_location %>
</p>

<h2>The Closest Buses Are...</h2>

<% if @bus_count == 0 %>
  <p>
    ...not really that close. Better order a Lyft. Or an Uber. Or call a friend. Or just give up, go back inside, and binge watch Netflix.
  </p>
<% end %>

<% @nearby_buses.each do |bus| %>
  <p>
    <strong>Route</strong>: <%= bus["ROUTE"] %><br>
    <strong>Vehicle</strong>: <%= bus["VEHICLE"] %><br>
    <strong>Next Stop</strong>: <%= bus["TIMEPOINT"] %><br>
    <strong>Direction</strong>: <%= bus["DIRECTION"] %><br>
  </p>
<% end %>

<%= link_to 'Edit', edit_location_path(@location) %> |
<%= link_to 'New Request', new_location_path %> |
<%= link_to 'Recent Requests', locations_path %>
